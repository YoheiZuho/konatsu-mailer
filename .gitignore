# Build artifacts
/server
*.exe

# PostgreSQL bind-mount data (docker-compose)
/db_data/

# Environment / secrets
.env
.env.local
*.env

# Go
*.test
coverage.out

# Node (web/ has its own .gitignore too)
node_modules/

# OS / editor
.DS_Store
*.log
